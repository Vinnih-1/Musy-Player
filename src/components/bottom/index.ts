import { BottomButton } from './bottom-button';
import { BottomContainer } from './bottom-container';
import { BottomDetails } from './bottom-details';
import { BottomImage } from './bottom-image';
import { BottomRoot } from './bottom-root';

export const Bottom = {
  Root: BottomRoot,
  Image: BottomImage,
  Button: BottomButton,
  Details: BottomDetails,
  Container: BottomContainer,
};
